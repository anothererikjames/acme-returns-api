$kind: http-example
request:
  url: "{{baseUrl}}/returns?status=completed&orderId=<string>"
  method: GET
  headers:
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    status: completed
    orderId: <string>
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
